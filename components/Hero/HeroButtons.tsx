import React, { FC } from "react";

const HeroButtons: FC = () => {
    return (
        <div className="hero__wrapper__text__buttons">
            <button className="btn">Get Started</button>
        </div>
    );
};

export default HeroButtons;
