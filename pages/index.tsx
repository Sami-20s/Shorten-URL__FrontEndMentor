import React, { FC } from "react";
import Hero from "../components/Hero/Hero";

const Home: FC = () => {
    return (
        <>
            <main>
                <Hero />
            </main>
        </>
    );
};

export default Home;
